{
  "name": "app",
  "private": true,
  "scripts": {
    "build": "esbuild app/javascript/*.* --bundle --sourcemap --format=esm --outdir=app/assets/builds --public-path=/assets --external:@hotwired/stimulus"
  },
  "dependencies": {
    "@hotwired/turbo-rails": "^8.0.4",
    "nodejs": "^0.0.0"
  }
}
